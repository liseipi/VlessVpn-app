<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_vpn"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_vpn"
        android:supportsRtl="true"
        android:theme="@style/Theme.VlessVPN">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Handle vless:// URI scheme import -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vless" />
            </intent-filter>
        </activity>

        <!-- VLESS VPN Service -->
        <service
            android:name=".VlessVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <!--
                FIX: Android 14+ 要求 foregroundServiceType="specialUse" 的 Service
                必须声明此 property，否则启动时抛 SecurityException。
            -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
        </service>

        <!-- VLESS SOCKS5 proxy service (runs the WebSocket tunnel) -->
        <service
            android:name=".VlessProxyService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!--
                FIX: Android 14+ 同上，必须声明 specialUse subtype property。
            -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn_proxy" />
        </service>

    </application>

</manifest>